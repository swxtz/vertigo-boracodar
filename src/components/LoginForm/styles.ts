import styled from "styled-components";

export const Container = styled.div`
  margin: 2.5rem 13rem 13rem 7rem;

  .header {
    margin-top: 4rem;
  
    h2 {
      color: var(--grey-800);
      font-weight: 700;
      font-size: 36px;
    }

    p {
      padding-top: 1rem;
      color: var(--grey-600);
      font-size: 1rem;
    }
  }
`;
